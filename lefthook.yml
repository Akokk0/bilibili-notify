pre-commit:
  commands:
    lint:
      glob: "*.{ts,tsx,js,jsx,mjs,css}"
      run: vpx biome check --staged --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
