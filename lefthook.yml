pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,js,json}"
      run: npx biome check --staged --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
