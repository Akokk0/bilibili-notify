name: CI

on:
  push:
    # dev-next 是 1.0 的开发线(发 alpha 时 dev 快进过去),和 dev 一样每次推送都跑门禁。
    branches: [dev, dev-next]
  pull_request:
    branches: [main]

jobs:
  # 门禁本体(Biome + build + typecheck + test)在 gate.yml —— 三条发布路径共用同一份
  # 定义,见那里的注释。
  gate:
    uses: ./.github/workflows/gate.yml
